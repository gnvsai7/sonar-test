/* eslint-disable eslint-comments/disable-enable-pair */
/* eslint-disable no-undef */
import '@testing-library/jest-dom';
import { fireEvent, render, screen } from '@testing-library/react';
import EntryStepper from './EntryStepper';

describe('Entry Stepper Tests', () => {
  it('should match snapshot on render', () => {
    // const mock1 = jest.fn();
    // const mock2 = jest.fn();
    // render(
    //   <EntryStepper emitActiveState={mock1} setValues={mock2}></EntryStepper>
    // );
    // const stepper = screen.getByTestId('stepper');
    // expect(stepper).toBeVisible();
    // const dropdownHeading = screen.getByTestId('dropdownHeading');
    // expect(dropdownHeading).toBeTruthy();
    // const nextButton = screen.getByTestId('nextButton');
    // expect(nextButton).toBeTruthy();
  });
  test('On click next should display back button', () => {
    // const mock1 = jest.fn();
    // const mock2 = jest.fn();
    // render(
    //   <EntryStepper emitActiveState={mock1} setValues={mock2}></EntryStepper>
    // );
    // const logo = screen.getByTestId('logo');
    // expect(logo).toBeVisible();
    // const stepper = screen.getByTestId('stepper');
    // expect(stepper).toBeVisible();
    // const dropdownHeading = screen.getByTestId('dropdownHeading');
    // expect(dropdownHeading).toBeTruthy();
    // const nextButton = screen.getByTestId('nextButton');
    // expect(nextButton).toBeTruthy();
    // fireEvent.click(nextButton);
    // const backButton = screen.getByTestId('backButton');
    // expect(backButton).toBeTruthy();
    // fireEvent.click(backButton);
    // expect(backButton).not.toBeVisible();
  });
});
